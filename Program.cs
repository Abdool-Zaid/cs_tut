namespace Cs_tut
{
    public class Program{

        static void Main(string[] args)
        {

            String input_time;
            System.Console.WriteLine("please enter a time ");
            input_time = System.Console.ReadLine(); 

            // if(new DateTime)// TODO:  Write a program and ask the user to enter a time value in the 24-hour time format (e.g. 19:00). A valid time should be between 00:00 and 23:59. If the time is valid, display "Ok"; otherwise, display "Invalid Time". If the user doesn't provide any values, consider it as invalid time.

        }
    }
    
}